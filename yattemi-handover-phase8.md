# YatteMi! Phase 8 引継ぎ資料

## プロジェクト情報
- **サービス**: 大学生向けスキルシェアプラットフォーム
- **技術**: Next.js 14 + TypeScript + Tailwind CSS + Supabase + Vercel
- **本番URL**: https://yattemi.vercel.app
- **ローカル**: `C:\Users\wakel\Documents\ohkita\GitHub\yattemi`

---

## Phase 8 完了内容

### 1. ルート整理
| 作業 | 詳細 |
|------|------|
| 削除 | `/challenges`, `/support` ディレクトリ |
| 統合 | レビューページを `/matches/[id]/review` に統合 |

**最終構造:**
```
/matches/[id]        → チャット
/matches/[id]/review → レビュー（統合）
```

---

### 2. UI改善 - explore ページ (`src/app/(main)/explore/page.tsx`)

| 変更 | Before | After |
|------|--------|-------|
| ヘッダー | タイトル+説明文 | 削除（スッキリ） |
| 検索バー | 幅いっぱい | 中央配置、`max-w-sm` |
| 日付ボタン | `rounded-full` 大きい | `rounded-md` コンパクト |
| カレンダー | 別位置 | 検索バー下にオーバーレイ |
| 余白 | `py-8`, `mb-8` | `py-3`, `mb-4` |
| 背景 | 白 | `bg-gray-50`（スティッキーヘッダー） |

**スティッキーヘッダー構造:**
```tsx
<div className="sticky top-16 z-20 bg-gray-50 -mx-4 px-4 py-2">
  {/* 検索バー - 中央配置 */}
  {/* 日付ボタン - 中央配置 */}
  {/* カレンダー（showDatePicker時） */}
</div>
```

---

### 3. UI改善 - ホームページ (`src/app/(main)/page.tsx`)

| 変更 | Before | After |
|------|--------|-------|
| バナー余白 | `p-6 mb-8` | `p-4 mb-4` |
| バナー角丸 | `rounded-2xl` | `rounded-lg` |
| カード間余白 | `gap-4` | `gap-1` |
| タイトル | `text-2xl` | `text-xl` |

---

### 4. ダッシュボード刷新 (`src/app/(main)/dashboard/page.tsx`)

**完全リニューアル:**

| Before | After |
|--------|-------|
| 統計カード（投稿数、閲覧数など） | 管理画面特化 |
| プロフィール表示 | 削除（別ページにある） |
| バッジ表示 | 削除 |

**新構造:**
```
┌─────────────────┬─────────────────┐
│ 🎓 サポート      │ 📚 チャレンジ    │
│                 │                 │
│ ▌募集中         │ ▌募集中         │
│  投稿1  👤👤    │  投稿1  👤      │
│  投稿2  👤      │                 │
│                 │                 │
│ ▌応募中         │ ▌応募中         │
│  投稿名     ›   │  なし           │
│                 │                 │
│ ▌サポート中     │ ▌チャレンジ中    │
│  投稿名  👤  ›  │  投稿名  👤  ›  │
└─────────────────┴─────────────────┘
┌─────────┬─────────┐
│ 🔔 通知  │ ⚙️ 設定 │
└─────────┴─────────┘
     プロフィールを見る →
```

**データ取得:**
```typescript
interface DashboardData {
  myPosts: DashboardPost[];        // 募集中の投稿 + 応募者アバター
  myApplications: DashboardApplication[];  // 応募中（返事待ち）
  myMatches: DashboardMatch[];     // マッチ中 + 相手アバター
}
```

**UIの特徴:**
- iOS風リストデザイン（`divide-y` で行間に線）
- バーで見出し強調（色分け: orange/blue/green/cyan/purple）
- 折りたたみ機能（3件以上で「あとN件」）
- 展開時は全件表示
- アイコン `h-8 w-8`、文字 `text-xs`

---

### 5. ナビゲーション改善

#### ボトムナビ追加 (`src/components/layout/bottom-nav.tsx`)
```
┌─────────────────────────────┐
│ 🏠    🔍    ➕    📋    👤  │
│ホーム  探す  投稿  管理  マイ │
└─────────────────────────────┘
```

| 項目 | 特徴 |
|------|------|
| 表示条件 | ログイン時のみ、スマホのみ（`md:hidden`） |
| 投稿ボタン | 中央、浮き上がり（`-mt-4`）、グラデーション |
| アクティブ | オレンジ色 |

#### ヘッダー修正 (`src/components/layout/header.tsx`)
- ドロップダウンに「管理」リンク追加

#### レイアウト修正 (`src/app/(main)/layout.tsx`)
```tsx
<main className="flex-1 pb-16 md:pb-0">{children}</main>
<div className="hidden md:block">
  <Footer />
</div>
<BottomNav />
```

| デバイス | フッター | ボトムナビ |
|----------|---------|-----------|
| PC | 表示 | 非表示 |
| スマホ | 非表示 | 表示 |

---

## ファイル変更一覧

| ファイル | 変更内容 |
|----------|---------|
| `src/app/(main)/explore/page.tsx` | スティッキーヘッダー、検索バー中央配置 |
| `src/app/(main)/page.tsx` | 余白削減、コンパクト化 |
| `src/app/(main)/dashboard/page.tsx` | 完全リニューアル |
| `src/app/(main)/layout.tsx` | ボトムナビ追加、フッター条件表示 |
| `src/components/layout/bottom-nav.tsx` | 新規作成 |
| `src/components/layout/header.tsx` | 管理リンク追加 |

---

## 残タスク

| 優先度 | タスク | 詳細 |
|--------|--------|------|
| 高 | IntersectionObserver修正 | 無限スクロールが動かない |
| 高 | 投稿の文字数制限確認 | `truncate` で切り詰めのみ、制限なし？ |
| 中 | レビュー導線の動作確認 | `/matches/[id]/review` が正しく動くか |
| 中 | 質問の通報機能 | 未実装 |
| 低 | TypeScript any型の修正 | Supabase型生成 |

---

## 技術メモ

### Tailwind CSS クラス
| 用途 | クラス |
|------|--------|
| 薄いグレー背景 | `bg-gray-50` または `bg-[#fcfcfc]` |
| スティッキー | `sticky top-16 z-20` |
| 行間の線 | `divide-y` |
| アイコン重ね | `flex -space-x-1.5` |
| iOS風矢印 | `›` (文字) |
| 縦バー | `w-1 h-4 bg-{color}-400 rounded-full` |

### データ取得パターン（ダッシュボード）
```typescript
// 募集中の投稿 + 応募者
.from('posts')
.select(`
  id, title, type,
  applications (
    id, status,
    applicant:profiles!applications_applicant_id_fkey (
      id, avatar_url, display_name
    )
  )
`)
.eq('user_id', user.id)
.eq('status', 'open')

// マッチ中
.from('matches')
.select(`
  id,
  post:posts (id, title, type, user_id),
  senpai:profiles!matches_senpai_id_fkey (...),
  kouhai:profiles!matches_kouhai_id_fkey (...)
`)
.or(`senpai_id.eq.${user.id},kouhai_id.eq.${user.id}`)
.eq('status', 'active')
```

---

## 過去の引継ぎ資料
- Phase 7: `/mnt/user-data/uploads/yattemi-handover-phase7.md`
- Phase 6: `/mnt/user-data/uploads/YATTEMI_HANDOVER_v5.md`
- Phase 5: `/mnt/user-data/uploads/YATTEMI_HANDOVER_v4.md`

---

## 次回やること候補
1. ダッシュボードのデザイン微調整（必要なら）
2. 無限スクロール修正
3. 通知の内訳表示（応募/いいね/メッセージ別）
4. 投稿編集・削除機能をダッシュボードから
